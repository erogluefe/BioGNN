# Optional Dependencies for BioGNN
# These packages provide advanced features but are not required for basic functionality

# Computer Vision (may require compilation)
opencv-python>=4.8.0  # Full OpenCV with GUI support
# opencv-python-headless>=4.8.0  # OpenCV without GUI (lighter, still requires compilation on some systems)

# Data Augmentation
albumentations>=1.3.0  # Advanced image augmentation (requires opencv)

# Visualization & Monitoring
plotly>=5.14.0  # Interactive plots
tensorboard>=2.13.0  # Training visualization
wandb>=0.15.0  # Weights & Biases experiment tracking (requires Go: https://go.dev/doc/install)
umap-learn>=0.5.0  # UMAP dimensionality reduction for embedding visualization

# Data Formats
h5py>=3.9.0  # HDF5 file support

# Advanced Configuration Management
hydra-core>=1.3.0  # Advanced config composition and overrides

# Dataset Downloaders
kaggle>=1.5.0  # Download datasets from Kaggle (e.g., SOCOFing)
# Note: Requires API credentials from https://www.kaggle.com/settings/account

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0

# Installation:
# pip install -r requirements-optional.txt

# Or install individually as needed:
# pip install kaggle  # For Kaggle datasets
# pip install opencv-python  # For OpenCV (may take 10-15 min to compile)
# pip install albumentations  # For advanced augmentation
# pip install tensorboard  # For training visualization
# pip install wandb   # For experiment tracking (requires Go)
